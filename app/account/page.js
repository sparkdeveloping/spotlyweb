import { AccountApp } from "@/components/account-app";

export const metadata = { title: "Your account", description: "Manage your shared Spotly account and linked sign-in methods." };
export default function AccountPage() { return <AccountApp />; }
