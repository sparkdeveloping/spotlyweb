rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEVELOPMENT ONLY. Never deploy this ruleset to production.
    match /{allPaths=**} { allow read, write: if true; }
  }
}
